---
import BaseLayout from '@/layouts/BaseLayout.astro';
import TextBlock from '@/components/TextBlock.astro';
import Profiles from '@/components/Profiles.astro';
import ImageText from '@/components/ImageText.astro';
import { aboutIntro, aboutProfiles, aboutSections } from '@/data/about';

// Locale slice picked explicitly, the same way services.astro reads
// serviceCategories.en and its /es/ twin reads .es.
const intro = aboutIntro.en;
const profiles = aboutProfiles.en;
const sections = aboutSections.en;

const michael = '/michael.jpg';
const office = '/office.jpg';
---

<BaseLayout title="About" pageHeading="About" description="Learn about Candura — who we are and how we work.">
  <TextBlock heading={intro.heading} tagline={intro.tagline} paragraphs={intro.paragraphs} />

  <div class="about-sections">
    <Profiles columns={profiles} />
    <ImageText
      title={sections[0].title}
      description={sections[0].paragraphs}
      image={office}
      color="var(--pink)"
      reverse
    />
    <ImageText
      title={sections[1].title}
      description={sections[1].paragraphs}
      bullets={sections[1].bullets}
      image={michael}
      color="var(--yellow)"
      imageSize="60%"
    />
  </div>
</BaseLayout>
