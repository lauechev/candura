<footer>
  <div class="left">
    <span class="wordmark" role="img" aria-label="Candura"></span>
    <span class="descriptor">a creative technology studio & consultancy</span>
  </div>
  <div class="right">
    <a href="https://instagram.com/candura.studio" target="_blank" rel="noopener noreferrer">@candura.studio</a>
    <div class="copy">&copy; 2026 Candura</div>
  </div>
</footer>

<style lang="scss">
  footer {
    background-color: var(--green);
    color: var(--cream);
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    padding: 1rem 4rem;
  }

  .left {
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
  }

  // The Candura logo, recoloured cream via a mask over a cream fill.
  .wordmark {
    display: block;
    height: 1.5rem;
    aspect-ratio: 1301 / 330;
    background-color: var(--cream);
    -webkit-mask: url('/greenlogo.svg') no-repeat left center / contain;
    mask: url('/greenlogo.svg') no-repeat left center / contain;
  }

  // The footer is deliberately outside the type scale: these sizes are fine
  // print, optically matched to a wordmark that drops to 1rem tall on phones.
  // --fs-small (a fixed 16px) would render the text larger than the logo.
  .descriptor {
    font-size: 0.95rem;
    opacity: 0.85;
  }

  .copy {
    font-size: 0.85rem;
    opacity: 0.6;
    margin-top: 0.5rem;
    padding-top: 0.3rem;
  }

  .right a {
    font-size: 0.95rem;
    color: var(--cream);
    opacity: 0.85;
    transition: opacity 150ms ease;

    &:hover {
      opacity: 1;
    }
  }

  // ── Tablet ────────────────────────────────────────────────────────────────
  @media (max-width: 1024px) {
    footer {
      padding: 0.9rem 2.5rem;
    }

    .wordmark {
      height: 1.3rem;
    }

    .descriptor {
      font-size: 0.8rem;
    }

    .right a {
      font-size: 0.8rem;
    }

    .copy {
      font-size: 0.75rem;
    }
  }

  // ── Mobile landscape / small tablet ───────────────────────────────────────
  @media (max-width: 768px) {
    footer {
      padding: 0.8rem 1.5rem;
    }

    .wordmark {
      height: 1.15rem;
    }

    .descriptor {
      font-size: 0.72rem;
    }

    .right a {
      font-size: 0.72rem;
    }

    .copy {
      font-size: 0.68rem;
      margin-top: 0.3rem;
    }
  }

  // ── Mobile portrait ───────────────────────────────────────────────────────
  @media (max-width: 480px) {
    footer {
      gap: 1rem;
      padding: 0.75rem 1.2rem;
      width: 100%;
    }

    .wordmark {
      height: 1rem;
    }

    .descriptor {
      font-size: 0.65rem;
    }

    .right {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 0.3rem;
    }

    .right a {
      font-size: 0.65rem;
    }

    .copy {
      font-size: 0.6rem;
      margin-top: 0;
    }
  }
</style>
