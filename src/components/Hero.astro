---
interface Props {
  tagline: string;
}

const { tagline } = Astro.props;
---

<section class="hero">
  <canvas id="node-canvas"></canvas>
  <div class="hero-content">
    <h1 class="tagline">{tagline}</h1>
  </div>
</section>

<!-- <script>
  import { NodeNetwork } from '@/scripts/NodeNetwork';

  const canvas = document.getElementById('node-canvas') as HTMLCanvasElement;
  const network = new NodeNetwork(canvas);
  network.start();

  window.addEventListener('pagehide', () => network.destroy());
</script> -->

<!-- <script>
  import { FloatingImages } from '@/scripts/FloatingImages';
  import computerSrc from '../../assets/computer.png';

  const canvas = document.getElementById('node-canvas') as HTMLCanvasElement;
  const floater = new FloatingImages(canvas, computerSrc.src);
  floater.start();

  window.addEventListener('pagehide', () => floater.destroy());
</script> -->

<!-- <script>
  import { BouncingImages } from '@/scripts/BouncingImages';
  import computerSrc from '../../assets/computer.png';

  const canvas = document.getElementById('node-canvas') as HTMLCanvasElement;
  const bouncer = new BouncingImages(canvas, computerSrc.src);
  bouncer.start();

  window.addEventListener('pagehide', () => bouncer.destroy());
</script> -->

<!-- <script>
  import { MouseFollowImages } from '@/scripts/MouseFollowImages';
  import computerSrc from '../../assets/computer.png';

  const canvas = document.getElementById('node-canvas') as HTMLCanvasElement;
  const follower = new MouseFollowImages(canvas, computerSrc.src);
  follower.start();

  window.addEventListener('pagehide', () => follower.destroy());
</script> -->

<script>
  import { GravitySpheres } from '@/scripts/GravitySpheres';

  const canvas = document.getElementById('node-canvas') as HTMLCanvasElement;
  const sim = new GravitySpheres(canvas);
  sim.start();

  window.addEventListener('pagehide', () => sim.destroy());
</script>

<style lang="scss">
  .hero {
    position: relative;
    width: 100%;
    height: 100vh;
    overflow: hidden;
  }

  canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
  }

  .hero-content {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    pointer-events: none;
    padding-bottom: 10%;
  }

  .tagline {
    font-family: var(--font-display);
    font-size: 3.5em;
    color: var(--black);
    font-weight: 500;
    margin: 0;
    // background-color: rgba(255, 255, 255, 0.8);
    padding: 0em 0.1em 1em;
    // border-radius: 8px;
  }

  // ── Tablet ────────────────────────────────────────────────────────────────
  @media (max-width: 1024px) {
    .hero {
      height: 98vh;
    }

    .tagline {
      font-size: 3em;
      line-height: 1.1;
      width: 50%;
      // padding: 0rem 1.8rem 2rem 1.8rem;
    }
  }

  // ── Mobile landscape / small tablet ───────────────────────────────────────
  @media (max-width: 768px) {
    .hero {
      height: 92vh;
    }

    .tagline {
      font-size: 3rem;
      line-height: 1.05;
      width: 80%;
      padding-bottom: 4rem;
    }
  }

  // ── Mobile portrait ───────────────────────────────────────────────────────
  @media (max-width: 480px) {
    .hero {
      height: 95vh;
    }

    .tagline {
      font-size: 2.4rem;
      line-height: 1;
      width: 85%;
      padding-bottom: 5rem;
    }
  }
</style>
